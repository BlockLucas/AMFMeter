package org.mule.util

//import AMFMeter.BuildInfo

object DependenciesUtils {

//  def getDependencyVersion(dependencyName: String): String = {
//    val dependencies = BuildInfo.allDependencies.map(dl => dl.split(':')).map(d => d(1) -> d(2)).toMap
//    dependencies(dependencyName)
//  }

}
